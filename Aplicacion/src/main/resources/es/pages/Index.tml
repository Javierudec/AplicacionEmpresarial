<html t:type="layout" title="Hi/Lo Guess"
  xmlns:t="http://tapestry.apache.org/schema/tapestry_5_3.xsd"
  xmlns:p="tapestry:parameter">
     
<!-- 
Para ingresar como administrador, hacer Login con username: admin, password: admin01
<ul>
	<t:if test="username">
		<t:if test="isAdmin">
			<li><t:pagelink page="AddMovie">Add Movie</t:pagelink></li>
		</t:if>
		<li><t:actionLink t:id="logout">Logout</t:actionLink></li>
		<p:else>
	    	<li><t:pagelink page="AccountCreation">Create account</t:pagelink></li>
	    	<li><t:pagelink page="Login">Login</t:pagelink></li>
		</p:else>
	</t:if>
</ul>

<tr/>

<h1>Movies</h1>
<ul>
	<t:Loop t:source="lastMovies" value="movieName">
		<li><t:actionLink t:id="viewProfile" t:context="movieName.name">${movieName.name} (${movieScore}/5)</t:actionLink></li>
	</t:Loop>
</ul>
-->
<div id="widgetContainer">
	<div id="upperWidgetContainer">			
		<div id="recommenderWidget">
			<t:If t:test="isLoggedIn">
				<h3>Recomendaciones</h3>
				<t:Loop t:source="recommendedMovies" value="recoMovie">
					<div id="recommendedMovie">
						<t:actionLink t:id="viewProfileRecoList" t:context="recoMovie.name"><h3>${recoMovie.name}</h3></t:actionLink>
						<img id="moviePoster" src="${recoMovieImage}"/>
						<img id="stars" src="${imageMovieScore}"/>
					</div>
				</t:Loop>
			</t:If>
		</div>
		<div class="menu_simple" id="debutMoviesWidget">
			<h1>Estrenos recientes</h1>
			<ul>
				<t:Loop t:source="debutMovies" value="movieName">
					<li><t:actionLink t:id="viewProfileLastAdded" t:context="movieName.name">${movieName.name}</t:actionLink></li>
				</t:Loop>
			</ul>
		</div>
		<div class="menu_simple" id="newMoviesWidget">

			<h1>Recien agregados</h1>
			<ul>
				<t:Loop t:source="lastMovies" value="movieName">
					<li><t:actionLink t:id="viewProfile" t:context="movieName.name">${movieName.name}</t:actionLink></li>
				</t:Loop>
			</ul>
		</div>
	</div>
		
	<div id="lowerWidgetContainer">
		<div class="menu_simple" id="recommededTagForSearch">
			<h1>Tags</h1>
			<ul>
				<t:Loop t:source="tags" value="tag">
					<li><t:actionLink t:id="viewSearch" t:context="tag">${tag}</t:actionLink></li>
				</t:Loop>
			</ul>
		</div>
		<div id="randomNews">
				<h3>Movie of the month</h3>
				<div id="movieOfMonth">
					<t:actionLink t:id="viewMovieOfTheMonth"><h3>${momName}</h3></t:actionLink>
					<img class="momPoster" src="${momImage}"/>
					<span class="description">${momDescription}</span>
					<img class="starsClass" src="${momScore}"/>
				</div>
		</div>
	</div>			
</div>
 
</html>